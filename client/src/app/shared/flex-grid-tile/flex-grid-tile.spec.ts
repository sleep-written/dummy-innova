import { FlexGridTile } from './flex-grid-tile';

describe('FlexGridTile', () => {
  it('should create an instance', () => {
    const directive = new FlexGridTile();
    expect(directive).toBeTruthy();
  });
});
